-- Snippet to understand the aggregate functions

SELECT min(marks)  -- min function is to find the minimum of group
FROM students;

SELECT max(marks)  -- min function is to find the maximum of group
FROM students;

SELECT avg(marks)  -- min function is to find the average of group
FROM students;

SELECT sum(marks)  -- sum function is to find the total of group
FROM students;
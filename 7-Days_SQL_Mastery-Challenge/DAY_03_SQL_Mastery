create table employee(
  emp_id int primary key,
  name varchar(15) not null,
  department varchar(15),
  salary float not null,
  city varchar(10)
);

insert into employee values (101,'naveen','finance',50000,'hubli');
insert into employee values (102,'ritvik','it',40000,'badami');
insert into employee values (103,'akash','it',30000,'davangere');
insert into employee values (104,'muttu','webinar',80000,'byadagi');
insert into employee values (105,'harish','webinar',70000,'bagalkote');

-- Display the number of employees in each department.

select count(emp_id),department
from employee
group by department;

-- Display the average salary of each department.

select avg(salary),department
from employee
group by department;

-- Display the highest salary in each department.

select max(salary),department
from employee
group by department;

-- Display departments having more than 2 employees.

select count(emp_id),department
from employee
group by department
having count(emp_id)>2;

-- Display departments whose average salary is greater than 60,000

select avg(salary),department
from employee
group by department
having avg(salary)>60000;












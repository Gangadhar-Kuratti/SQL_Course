create table employee(
  emp_id int primary key,
  name varchar(15) not null,
  department varchar(15),
  salary float not null,
  city varchar(10)
);

insert into employee values (101,'naveen','finance',50000,'hubli');
insert into employee values (102,'ritvik','tech',40000,'badami');
insert into employee values (103,'akash','it',30000,'davangere');
insert into employee values (104,'muttu','webinar',80000,'byadagi');
insert into employee values (105,'harish','seminar',70000,'bagalkote');

select * from employee;

select max(salary)
from employee;

select avg(salary)
from employee;

select min(salary)
from employee;

select Count(name)
from employee;

select sum(salary)
from employee;
